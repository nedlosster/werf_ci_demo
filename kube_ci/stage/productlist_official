# Шаблон. Скопируйте в ./productlist и при необходимости отредактируйте.
# Формат: [<product>]=<env>, где <product> -- каталог в ../../apps,
# а <env> -- имя shell-функции в .helm/def.sh продукта (здесь -- stage).

#!/bin/bash

declare -A PRODUCTS

PRODUCTS=(
    [cmdb-web]=stage
    [asset-svc]=stage
)
