# Шаблон. Скопируйте в ./productlist и при необходимости отредактируйте.
# Формат: [<product>]=<env>, где <product> -- каталог в ../../apps,
# а <env> -- имя shell-функции в .helm/def.sh продукта (здесь -- prod).

#!/bin/bash

declare -A PRODUCTS

PRODUCTS=(
    [app1-java-react]=prod
    [app2-python-angular]=prod
)
