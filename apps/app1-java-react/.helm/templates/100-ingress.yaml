apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: app1-java-react
  labels:
    {{- include "app1.labels" . | nindent 4 }}
  annotations:
    nginx.ingress.kubernetes.io/ssl-redirect: "false"
spec:
  ingressClassName: nginx
  rules:
    - host: {{ .Values.domain | quote }}
      http:
        paths:
          - path: /api
            pathType: Prefix
            backend:
              service:
                name: app1-java-react-backend
                port:
                  number: {{ .Values.backend.port }}
{{- if .Values.pgadmin.enabled }}
          - path: /pgadmin
            pathType: Prefix
            backend:
              service:
                name: app1-java-react-pgadmin
                port:
                  number: 80
{{- end }}
          - path: /
            pathType: Prefix
            backend:
              service:
                name: app1-java-react-frontend
                port:
                  number: {{ .Values.frontend.port }}
