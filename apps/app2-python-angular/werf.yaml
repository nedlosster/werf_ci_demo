project: app2-python-angular
configVersion: 1
cleanup:
  disableGitHistoryBasedPolicy: true
  keepPolicies:
    - references:
        branch: /.*/
      imagesPerReference:
        last: 2

{{- range $path, $content := .Files.Glob ".werf-partial/*.yaml" }}
---
{{ tpl $content $ }}
{{- end }}
